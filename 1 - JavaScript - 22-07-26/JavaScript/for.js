// for(let contador = 0; contador <= 100; contador++){
//     let resultado = contador % 2 == 0 ? "Numero par:":"numero impar:"
//     console.log(resultado,contador)
// }

const word = 'JavaScript'
for (let indice = 0; indice <= word.length; indice++){
    console.log(word.slice(indice,word.length))
}